main() {
  // var myName = 'Sarthak';
  // for (var letter in myName.split('')) {
  //   print(letter);
  // }
  var names = ['Sarthak', 'John', 'Jeny', 'Jackson', 'Joe', 'Jolly'];
  var searchTerm = 'jO';
  // names.forEach((name) {
  //   if (name.toLowerCase().contains(searchTerm.toLowerCase())) {
  //     print(name);
  //   }
  // });
  // for (var name in names) {
  //   if (name.toLowerCase().contains(searchTerm.toLowerCase())) {
  //     print(name);
  //   }
  // }
  // for (var i = 0; i < names.length; i++) {
  //   print(names[i]);
  // }
}
