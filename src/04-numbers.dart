main() {
  var pi = 'the value of pi is 3.1419';
  print(pi.runtimeType);
  var newPI = double.parse(pi);
  print(newPI.runtimeType);
  // number 64 bit
  // int bitcoinPrice = 240000;
  // double bitcoinExactPrice = 24000.2342;

  // print('Bitcoin price is :' + bitcoinPrice.toString());
  // print('Bitcoin exact price is :' + bitcoinExactPrice.toStringAsFixed(2));
}
