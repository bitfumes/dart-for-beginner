main() {
  var name = 'Sarthak Shrivastava';
  // print(name.isNotEmpty);
  // print(name.codeUnits);
  // print(name.toUpperCase());
  // print(name.toLowerCase());
  // print(name.length);
  // print(name.trim().length);
  // print(name.replaceAll('Sarthak', 'John'));
}
